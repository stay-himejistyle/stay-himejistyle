---
import { getEntry, render } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";

const entry = await getEntry("legal", "privacy-policy");
const { Content } = await render(entry);
---

<BaseLayout
  title="プライバシーポリシー | STAY HIMEJI by 姫路スタイル"
  description="STAY HIMEJI by 姫路スタイルのプライバシーポリシー。取得する情報・利用目的・Cookieの使用について定めています。"
>
  <main class="mx-auto max-w-2xl px-6 py-16">
    <h1 class="text-3xl font-bold text-stone-900">{entry.data.title}</h1>
    <p class="mt-2 text-sm text-stone-500">最終更新：{entry.data.lastUpdated}</p>
    <div class="prose prose-stone mt-8 max-w-none">
      <Content />
    </div>
  </main>
</BaseLayout>
